# 🛡️ HappVPN — установка на VPS

## Что это?
Своя VPN-панель с админкой. Берёшь чужие VLESS ключи, пропускаешь через свой сервер (relay), раздаёшь подписки клиентам. Они видят только твой сервер.

---

## 📋 Требования
- VPS (Ubuntu 22.04+), минимум 1 CPU / 512MB RAM
- Домен с A-записью на IP сервера
- Открытый порт `80` на время выпуска TLS-сертификата
- VLESS ключи (купленные или свои)

---

## 1️⃣ Подключись к серверу
```bash
ssh root@ТВОЙ_IP
```

## 2️⃣ Подготовь домен
До установки проверь, что домен уже указывает на сервер:
```bash
dig +short vpn.example.com
```
Ответ должен быть IP твоего VPS. Если DNS ещё не обновился, установщик не сможет выпустить сертификат.

## 3️⃣ Загрузи проект на сервер
С локального компьютера:
```bash
cd "C:\Users\ИМЯ\Desktop\HappVPN"
ssh root@ТВОЙ_IP "mkdir -p /root/happvpn"
scp -r .\* root@ТВОЙ_IP:/root/happvpn/
```

На сервере:
```bash
cd /root/happvpn
```

## 4️⃣ Запусти установщик
```bash
bash install.sh vpn.example.com
```
Установщик сам поставит Node.js, Xray, PM2, зависимости из `package.json`, выпустит TLS-сертификат и запустит панель.

## 5️⃣ Открой панель
В конце установки ты увидишь адрес и пароль:
```
🌐 Панель: https://vpn.example.com/panel-xxxxxxxxxxxxxxxx
🔑 Пароль: xxxxxxxxxxxxxxxx
```

Если нужно посмотреть адрес и пароль ещё раз:
```bash
node -e "const s=require('/opt/happvpn/data.json').settings; console.log(s.serverUrl + '/' + s.adminPath); console.log(s.adminPassword);"
```

## 6️⃣ Проверь сервисы
```bash
pm2 status
systemctl status xray --no-pager
```

---

## ⚙️ Настройка

### В панели:
1. **Настройки → URL сервера** — уже должен стоять `https://vpn.example.com`
2. **Настройки → Relay** — уже должен быть включен после `install.sh`
   - Домен relay: `vpn.example.com`
   - Основной порт: `8443`
   - Начальный порт: `10001`
3. **Шаблоны** → добавь VLESS-ключи (URI)
4. **Подписки** → создай подписку, скопируй URL
5. **Дай URL клиенту** — он добавляет в Happ/v2rayN/Streisand

---

## 🌐 Nginx (по желанию)

Обычная установка уже запускает панель на `https://vpn.example.com`, поэтому Nginx не нужен.

Если всё-таки нужен reverse proxy, используй готовый скрипт. Он перезапустит HappVPN в режиме `HAPPVPN_HTTP_ONLY=1` на локальном порту `3000`, чтобы Node.js не занимал `80/443`.

```bash
bash add-nginx.sh vpn.example.com
```

---

## 🔥 Откройте порты
`install.sh` добавляет правила сам, но если firewall настраиваешь вручную:
```bash
# Панель HTTPS и выпуск сертификата
ufw allow 80/tcp
ufw allow 443/tcp

# Xray основной
ufw allow 8443/tcp

# Relay порты (по количеству серверов)
ufw allow 10001:10100/tcp

ufw enable
```

---

## 📱 Клиенты VPN
- **iOS**: Streisand, Happ, V2Box
- **Android**: V2rayNG, Happ
- **Windows**: Happ, v2rayN, Nekoray
- **macOS**: V2Box

Клиент добавляет URL подписки → получает список серверов → подключается.

---

## 🛠️ Команды управления

```bash
# Статус
pm2 status

# Логи
pm2 logs happvpn

# Рестарт
pm2 restart happvpn

# Обновить файлы (после загрузки новых)
pm2 restart happvpn

# Бэкап данных
cp /opt/happvpn/data.json ~/backup-$(date +%Y%m%d).json
```

---

## ⚡ Возможности
- 🔄 Relay через свой сервер (чужие ключи → твой IP)
- 📊 Мониторинг подключений в реальном времени
- 🌍 Геолокация IP (город, страна, провайдер)
- 📈 Учёт трафика с лимитами (автоблок при превышении)
- 🚫 Блокировка подозрительных IP
- 🔔 Telegram-уведомления о подозрительных подключениях
- 📱 PWA — ставится как приложение на телефон
- 🛡️ Секретный URL панели
- 📋 Лимит устройств на подписку
- ⏰ Срок действия подписок

---

## ❗ Важно
- **Не потеряй `data.json`** — в нём все подписки и настройки
- **Секретный путь** генерируется один раз, сохрани его
- **Relay порты** открой в файрволе, иначе клиенты не подключатся
- **Xray управляется панелью** — не редактируй его конфиг вручную
